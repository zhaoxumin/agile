<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.agile.go.dao.PdfTransferWordMapper">
    <resultMap id="BaseResultMap" type="com.agile.go.entity.PdfTransferWordEntity">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="app_name" property="appName" jdbcType="VARCHAR"/>
        <result column="access_key" property="accessKey" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="findUrl" parameterType="com.agile.go.vo.TransferQueryVo" resultMap="BaseResultMap">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        select
        *
        from pdf_transfer_word t
        where t.id=#{id}
    </select>

    <insert id="insertPdf" parameterType="com.agile.go.entity.PdfTransferWordEntity" useGeneratedKeys="true"
            keyProperty="id">
        insert into
            pdf_transfer_word (id,
                               pdf_url,
                               word_url)
        values (#{id},
                #{pdfUrl},
                #{wordUrl})
    </insert>

    <update id="updateWord" parameterType="com.agile.go.entity.PdfTransferWordEntity">
        update pdf_transfer_word t
        set
        word_url = #{wordUrl}
        <trim prefix="WHERE" prefixOverrides="AND | OR ">
            <if test="id != null and id!='' ">
                and t.id=#{id}
            </if>
        </trim>
    </update>

</mapper>